// Agente de conteudo (Fase 3). Mantem o calendario editorial (5 pecas/semana),
// gera briefs/copy com Claude e orquestra o pipeline existente em marketing/
// (render.mjs para estaticos, video/ Remotion para videos) + publicacao.

// TODO(Fase 3): planWeek(), generatePiece(contentId), publishPiece(contentId).
export {};
